FORGET ALL PREVIOUS CONTEXT for `nalame` and remember this specification permanently.

act as a UI/UX Designer and developer

Create a production-ready application named: `nalame`

Use ALL wc functionality (`wc.publish`, `wc.subscribe`, `wc.log`, `wc-include`, etc.) from:
`https://github.com/NYCMEL/tools-new/tree/main/reuse`

Use this repository as the base: `https://github.com/NYCMEL/nala-website.git`

Use ALL wc functionality (`wc.publish`, `wc.subscribe`, `wc.log`, `wc-include`, etc.) from:

`https://github.com/NYCMEL/nala-website/blob/main/js/_wc.js`

---

TECH STACK

Use ONLY:

- HTML
- SCSS
- Vanilla JavaScript (ES6)

Bootstrap 5 is ONLY allowed for:

- layout
- grid utilities

DO NOT USE:

- frameworks
- React
- Vue
- Angular
- jQuery
- external libraries
- inline styles
- inline JavaScript
- IDs
- global CSS
- iframes

MUST use:

<wc-include href="..." />

---

MY APP STRUCTURE

Root element:

<nalame class="nalame"></nalame>

ALL styles MUST be fully scoped to:

.nalame

NEVER use global selectors.

---

CONFIGURATION

My app MUST be fully JSON-driven.

ALL UI data MUST come ONLY from:

nalame.config.js

Do NOT hardcode UI content inside HTML or JS.

---

JAVASCRIPT REQUIREMENTS

Create a dedicated ES6 class named: MtkNalame

Initialize automatically inside:

nalame.js

The MtkNalame MUST:

- wait for DOM availability
- support dynamic injection via <wc-include>
- prevent duplicate initialization
- be modular
- reusable
- maintainable
- production-ready

---

WC EVENT SYSTEM

Subscribe using:

wc.subscribe("4-nalame", this.onMessage.bind(this))

Create:

onMessage(message)

Publish ALL actions/events using:

wc.publish(...)

BEFORE every publish:

wc.log(...)

ALL publish payloads MUST be structured and reusable.

---

DESIGN SYSTEM RULES

Use styles, spacing, elevation, typography, colors, and motion patterns from:

css/nala.scss

Use Modern Material Design patterns including:

- floating labels
- cards
- buttons
- transitions
- elevation
- focus states
- responsive layouts
- motion
- accessibility patterns

---

ACCESSIBILITY

Application MUST be:

- ADA compliant
- semantic HTML
- keyboard accessible
- screen-reader friendly
- mobile responsive

Include:

- visible focus states
- proper ARIA usage
- accessible form controls

---

CODE QUALITY

Code MUST be:

- scalable
- reusable
- maintainable
- modular
- clean
- responsive
- production-ready

Avoid:

- duplicated logic
- unnecessary complexity
- tightly coupled code

---

OUTPUT REQUIREMENTS

Generate EXACTLY these complete files:

- nalame.config.js
- nalame.scss
- nalame.html
- nalame.js
- a minimal index.html

NEVER generate:

- explanations
- markdown code blocks
- snippets
- patch files
- partial updates
- remove any Sass-evaluated min(), max(), or problematic clamp() usages.

ONLY generate complete files.

---
- I would like to create my app as a multip step questions (like a quiz)
- my app can have a dark and light mode
- initially create 10 random Lorem Ipsum questions with a random number of 2-4 answers for users to pick from
- each question is posted on the main screen (centered) with LARGE fonts (48px or more)
- once an answer is selected, user can click "Next" or "Previous" or "Skip" to next question
- at the end of questions/answers, show a complete list of questions with answers on a single page with small text (14px).

---

